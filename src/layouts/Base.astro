---
import { profil, contacts } from "../data/cv.js";

const { title, description } = Astro.props;
const pageTitle = title
  ? `${title} · ${profil.nom}`
  : `${profil.nom} · ${profil.titre}`;
const pageDesc =
  description ??
  `Portfolio de ${profil.nom}, ${profil.titre.toLowerCase()}. Parcours, compétences et projets.`;

const nav = [
  { href: "/", label: "Accueil" },
  { href: "/cv", label: "CV" },
];
const path = Astro.url.pathname;
---

<!doctype html>
<html lang="fr">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="icon" href="/favicon.svg" type="image/svg+xml" />
    <title>{pageTitle}</title>
    <meta name="description" content={pageDesc} />
    <meta name="author" content={profil.nom} />
    <meta property="og:title" content={pageTitle} />
    <meta property="og:description" content={pageDesc} />
    <meta property="og:type" content="website" />
    <meta name="theme-color" content="#f97316" />
    <script is:inline>
      // Applique le thème avant le rendu pour éviter le flash
      const t = localStorage.getItem("theme");
      if (t === "dark" || (!t && matchMedia("(prefers-color-scheme: dark)").matches)) {
        document.documentElement.dataset.theme = "dark";
      }
    </script>
  </head>
  <body>
    <a class="skip" href="#main">Aller au contenu</a>

    <header class="site-header">
      <div class="wrap header-inner">
        <a href="/" class="brand" aria-label="Accueil">
          <span class="brand-mark">CP</span>
          <span class="brand-name">Christophe&nbsp;Pauliac</span>
        </a>
        <nav class="nav" aria-label="Navigation principale">
          {
            nav.map((n) => (
              <a href={n.href} class:list={["nav-link", { active: path === n.href }]}>
                {n.label}
              </a>
            ))
          }
        </nav>
        <button
          class="theme-toggle"
          id="theme-toggle"
          type="button"
          aria-label="Changer de thème"
          title="Thème clair / sombre"
        >
          <svg class="theme-icon sun" viewBox="0 0 24 24" width="18" height="18" aria-hidden="true" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round">
            <circle cx="12" cy="12" r="4" /><path d="M12 2v2M12 20v2M4.9 4.9l1.4 1.4M17.7 17.7l1.4 1.4M2 12h2M20 12h2M4.9 19.1l1.4-1.4M17.7 6.3l1.4-1.4" />
          </svg>
          <svg class="theme-icon moon" viewBox="0 0 24 24" width="18" height="18" aria-hidden="true" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M21 12.8A9 9 0 1 1 11.2 3a7 7 0 0 0 9.8 9.8z" />
          </svg>
        </button>
      </div>
    </header>

    <main id="main">
      <slot />
    </main>

    <footer class="site-footer">
      <div class="wrap footer-inner">
        <div>
          <p class="footer-name">{profil.nom}</p>
          <p class="footer-role">{profil.titre}</p>
        </div>
        <div class="footer-links">
          <a
            class="footer-link"
            id="email-link"
            data-user={contacts.emailUser}
            data-domain={contacts.emailDomain}
            href="#"
            rel="nofollow"
          >
            <svg class="footer-ico" viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
              <rect x="3" y="5" width="18" height="14" rx="2" /><path d="m3 7 9 6 9-6" />
            </svg>
            <span class="footer-label">Écrire un email</span>
          </a>
          <a class="footer-link" href={contacts.linkedin} target="_blank" rel="noopener">
            <svg class="footer-ico" viewBox="0 0 24 24" width="16" height="16" fill="currentColor" aria-hidden="true">
              <path d="M20.45 20.45h-3.56v-5.57c0-1.33-.02-3.04-1.85-3.04-1.85 0-2.13 1.44-2.13 2.94v5.67H9.35V9h3.41v1.56h.05c.48-.9 1.63-1.85 3.36-1.85 3.6 0 4.27 2.37 4.27 5.45v6.29zM5.34 7.43a2.07 2.07 0 1 1 0-4.14 2.07 2.07 0 0 1 0 4.14zM7.12 20.45H3.56V9h3.56v11.45zM22.22 0H1.77C.79 0 0 .77 0 1.72v20.55C0 23.23.79 24 1.77 24h20.45c.98 0 1.78-.77 1.78-1.73V1.72C24 .77 23.2 0 22.22 0z" />
            </svg>
            <span class="footer-label">LinkedIn</span>
          </a>
          {
            contacts.github && (
              <a class="footer-link" href={contacts.github} target="_blank" rel="noopener">
                <svg class="footer-ico" viewBox="0 0 24 24" width="16" height="16" fill="currentColor" aria-hidden="true">
                  <path d="M12 .3a12 12 0 0 0-3.8 23.4c.6.1.8-.3.8-.6v-2c-3.3.7-4-1.6-4-1.6-.6-1.4-1.4-1.8-1.4-1.8-1-.7.1-.7.1-.7 1.2 0 1.9 1.2 1.9 1.2 1 1.8 2.8 1.3 3.5 1 0-.8.4-1.3.7-1.6-2.7-.3-5.5-1.3-5.5-6 0-1.2.5-2.3 1.3-3.1-.2-.4-.6-1.6 0-3.2 0 0 1-.3 3.3 1.2a11.5 11.5 0 0 1 6 0C17.3 4.7 18.3 5 18.3 5c.6 1.6.2 2.8 0 3.2.9.8 1.3 1.9 1.3 3.2 0 4.6-2.8 5.6-5.5 5.9.5.4.9 1.1.9 2.3v3.3c0 .3.1.7.8.6A12 12 0 0 0 12 .3z" />
                </svg>
                <span class="footer-label">GitHub</span>
              </a>
            )
          }
          {
            contacts.codecrafting && (
              <a class="footer-link" href={contacts.codecrafting} target="_blank" rel="noopener">
                <img class="footer-ico footer-ico-img" src="/assets/codecrafting-icon.png" alt="" width="16" height="16" loading="lazy" />
                <span class="footer-label">CodeCrafting</span>
              </a>
            )
          }
        </div>
      </div>
      <p class="footer-copy wrap">© {new Date().getFullYear()} {profil.nom}</p>
    </footer>

    <script>
      // Toggle thème
      const btn = document.getElementById("theme-toggle");
      btn?.addEventListener("click", () => {
        const isDark = document.documentElement.dataset.theme === "dark";
        if (isDark) {
          delete document.documentElement.dataset.theme;
          localStorage.setItem("theme", "light");
        } else {
          document.documentElement.dataset.theme = "dark";
          localStorage.setItem("theme", "dark");
        }
      });

      // Email assemblé en JS → invisible pour les bots dans le HTML source
      const el = document.getElementById("email-link");
      if (el) {
        const addr = `${el.dataset.user}@${el.dataset.domain}`;
        el.setAttribute("href", `mailto:${addr}`);
        // Ne remplacer que le libellé, pas l'icône
        const label = el.querySelector(".footer-label");
        if (label) label.textContent = addr;
      }
    </script>

    <style is:global>
      :root {
        --bg: #ffffff;
        --bg-soft: #f6f8fa;
        --surface: #ffffff;
        --surface-2: #f1f4f7;
        --border: #e5e9ef;
        --text: #0f1722;
        --heading: #0b1220;
        --muted: #4a5a6e;
        --primary: #f97316;
        --primary-2: #ea670d;
        --primary-text: #c2560a;
        --accent: #1597a0;
        --accent-text: #0e7a82;
        --ok: #16a34a;
        --radius: 14px;
        --radius-sm: 10px;
        --maxw: 1120px;
        --shadow-sm: 0 1px 2px rgba(15, 23, 34, 0.04), 0 1px 3px rgba(15, 23, 34, 0.06);
        --shadow: 0 10px 30px rgba(15, 23, 34, 0.08);
        --shadow-lg: 0 20px 50px rgba(15, 23, 34, 0.12);
        --font: system-ui, -apple-system, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
        --mono: ui-monospace, "Cascadia Code", "Fira Code", Consolas, monospace;
      }

      :root[data-theme="dark"] {
        --bg: #0e1116;
        --bg-soft: #141922;
        --surface: #161c26;
        --surface-2: #1c2431;
        --border: #26303e;
        --text: #e7ecf3;
        --heading: #f4f7fb;
        --muted: #9fb0c4;
        --primary: #ff8a3d;
        --primary-2: #f97316;
        --primary-text: #ff9a55;
        --accent: #2bb6bf;
        --accent-text: #4fd0d8;
        --shadow-sm: 0 1px 2px rgba(0, 0, 0, 0.3);
        --shadow: 0 10px 30px rgba(0, 0, 0, 0.4);
        --shadow-lg: 0 20px 50px rgba(0, 0, 0, 0.5);
      }

      * { box-sizing: border-box; }

      html { scroll-behavior: smooth; }
      @media (prefers-reduced-motion: reduce) {
        html { scroll-behavior: auto; }
        * { animation-duration: 0.01ms !important; transition-duration: 0.01ms !important; }
      }

      body {
        margin: 0;
        font-family: var(--font);
        color: var(--text);
        background: var(--bg);
        line-height: 1.6;
        -webkit-font-smoothing: antialiased;
      }

      h1, h2, h3 { color: var(--heading); line-height: 1.15; letter-spacing: -0.02em; }

      a { color: var(--primary-text); }

      .wrap { width: 100%; max-width: var(--maxw); margin-inline: auto; padding-inline: 24px; }

      .skip {
        position: absolute; left: -9999px; top: 0; z-index: 100;
        background: var(--primary); color: #fff; padding: 10px 16px; border-radius: 0 0 8px 0;
      }
      .skip:focus { left: 0; }

      /* Header */
      .site-header {
        position: sticky; top: 0; z-index: 50;
        background: color-mix(in srgb, var(--bg) 85%, transparent);
        backdrop-filter: saturate(180%) blur(10px);
        border-bottom: 1px solid var(--border);
      }
      .header-inner { display: flex; align-items: center; gap: 20px; height: 64px; }
      .brand { display: inline-flex; align-items: center; gap: 10px; text-decoration: none; font-weight: 800; color: var(--heading); }
      .brand-mark {
        display: grid; place-items: center; width: 34px; height: 34px; border-radius: 9px;
        background: var(--primary); color: #fff; font-size: 0.85rem; letter-spacing: 0.02em;
        box-shadow: var(--shadow-sm);
      }
      .brand-name { font-size: 0.98rem; }
      .nav { margin-left: auto; display: flex; gap: 6px; }
      .nav-link {
        text-decoration: none; color: var(--muted); font-weight: 600; font-size: 0.95rem;
        padding: 8px 14px; border-radius: 999px;
      }
      .nav-link:hover { color: var(--heading); background: var(--surface-2); }
      .nav-link.active { color: var(--primary-text); background: color-mix(in srgb, var(--primary) 12%, transparent); }

      .theme-toggle {
        display: grid; place-items: center; width: 38px; height: 38px; border-radius: 999px;
        border: 1px solid var(--border); background: var(--surface); color: var(--heading);
        cursor: pointer; position: relative;
      }
      .theme-toggle:hover { border-color: var(--primary); }
      .theme-icon { position: absolute; transition: opacity 0.2s, transform 0.2s; }
      .sun { opacity: 0; transform: scale(0.6) rotate(-40deg); }
      .moon { opacity: 1; }
      :root[data-theme="dark"] .sun { opacity: 1; transform: none; }
      :root[data-theme="dark"] .moon { opacity: 0; transform: scale(0.6); }

      /* Footer */
      .site-footer { margin-top: 96px; border-top: 1px solid var(--border); background: var(--bg-soft); padding: 40px 0 28px; }
      .footer-inner { display: flex; flex-wrap: wrap; gap: 20px; justify-content: space-between; align-items: center; }
      .footer-name { margin: 0; font-weight: 800; color: var(--heading); }
      .footer-role { margin: 2px 0 0; color: var(--muted); font-size: 0.92rem; }
      .footer-links { display: flex; gap: 8px; flex-wrap: wrap; }
      .footer-link {
        display: inline-flex; align-items: center; gap: 8px;
        text-decoration: none; color: var(--heading); font-weight: 600; font-size: 0.9rem;
        padding: 8px 14px; border: 1px solid var(--border); border-radius: 999px; background: var(--surface);
      }
      .footer-link:hover { border-color: var(--primary); color: var(--primary-text); }
      .footer-ico { flex-shrink: 0; color: var(--muted); transition: color 0.12s; }
      .footer-link:hover .footer-ico { color: var(--primary-text); }
      .footer-ico-img { border-radius: 4px; object-fit: contain; }
      .footer-copy { margin: 22px auto 0; color: var(--muted); font-size: 0.82rem; }

      @media (max-width: 560px) {
        .brand-name { display: none; }
      }
    </style>
  </body>
</html>
